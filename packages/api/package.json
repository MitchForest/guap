{
  "name": "@guap/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --out-dir dist",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "pnpm run typecheck"
  },
  "dependencies": {
    "@guap/types": "workspace:*",
    "convex": "catalog:",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "tsup": "^8.2.4",
    "typescript": "^5.9.3"
  }
}
