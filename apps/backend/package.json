{
  "name": "@guap/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "generate": "CONVEX_DISABLE_TELEMETRY=1 convex codegen",
    "build": "pnpm run generate",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "pnpm run typecheck"
  },
  "dependencies": {
    "@better-auth/stripe": "latest",
    "@convex-dev/better-auth": "^0.9.1",
    "@guap/api": "workspace:*",
    "@guap/types": "workspace:*",
    "better-auth": "^1.3.27",
    "convex": "^1.27.5",
    "convex-helpers": "catalog:",
    "stripe": "^18.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "typescript": "^5.9.3"
  }
}
